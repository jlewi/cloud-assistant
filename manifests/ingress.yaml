apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cloud-assistant-ui
  namespace: cloud-assistant
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
  #   alb.ingress.kubernetes.io/certificate-arn: arn:aws:acm:us-east-2:381491863923:certificate/6848ab4f-21a4-4d28-a5ec-847e21f5915f
  #   alb.ingress.kubernetes.io/healthcheck-interval-seconds: "15"
  #   alb.ingress.kubernetes.io/healthcheck-port: 8443
  #   alb.ingress.kubernetes.io/healthcheck-protocol: HTTPS
  #   alb.ingress.kubernetes.io/healthcheck-timeout-seconds: "5"
  #   alb.ingress.kubernetes.io/healthy-threshold-count: "2"
  #   alb.ingress.kubernetes.io/scheme: internet-facing
  #   alb.ingress.kubernetes.io/success-codes: 200,302
  #   alb.ingress.kubernetes.io/target-type: ip
  #   alb.ingress.kubernetes.io/unhealthy-threshold-count: "2"
  #   alb.ingress.kubernetes.io/backend-protocol: HTTPS
    # kubernetes.io/ingress.class: alb
spec:
  tls:
  - hosts:
    - localhost
    secretName: tls-certs
  # ingressClassName: alb
  rules:
  - host: localhost
    http:
      paths:
      - pathType: Prefix
        path: /
        backend:
          service:
            name: cloud-assistant-ui
            port:
              number: 8443
